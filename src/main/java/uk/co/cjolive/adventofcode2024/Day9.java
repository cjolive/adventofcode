package uk.co.cjolive.adventofcode2024;

import uk.co.cjolive.Day;

public class Day9 extends Day {
    @Override
    public long task1(boolean isTest) {
        // 012345
        //        String input = "2333133121414131402";
        //        StringBuilder sb = new StringBuilder();
        //
        //        int id = 0;
        //        for (int i = 0; i < input.length(); i++) {
        //            if (i % 2 == 0) {
        //                sb.append(stringOfLength(id, Character.getNumericValue(input.charAt(i))));
        //            } else {
        //                sb.append(stringOfLength('.', Character.getNumericValue(input.charAt(i))));
        //            }
        //            id++;
        //        }
        //        System.out.println(sb.toString());

        return 0;
    }

    @Override
    public long task2(boolean isTest) {
        return 0;
    }

    //    private String stringOfLength(int id, int length) {
    //        if (length == 0)
    //            return "";
    //        char c = (char)(id + '0');
    //        return String.valueOf(c).repeat(length);
    //    }
}
